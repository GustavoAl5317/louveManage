# OpenAI (leitura de notas fiscais)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Vercel Postgres / Neon / qualquer Postgres
# Quando criar o Vercel Postgres no dashboard, ele injeta automaticamente:
POSTGRES_URL=postgres://user:pass@host/db?sslmode=require
